machine:
  timezone: Europe/Paris

  ruby:
    version: 2.2.2

  services:
    - sqlite3

  hosts:
    planning_gem.dev: 127.0.0.1

## Customize database setup
database:
  override:
    - echo "NOTHING TO BE DONE HERE"

## Customize test commands
test:
  override:
    - xvfb-run --auto-servernum bundle exec rake